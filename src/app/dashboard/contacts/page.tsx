import { lusitana } from '@/app/ui/fonts';

export default function ContactPage() {
    return (
      <div>
        <h1 className={`${lusitana.className} mb-4 text-xl md:text-2xl`}>Contact Page</h1>
      </div>
    )
  }