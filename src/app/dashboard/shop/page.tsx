import { lusitana } from '@/app/ui/fonts';

export default function ShopPage() {
    return (
      <div>
        <h1 className={`${lusitana.className} mb-4 text-xl md:text-2xl`}>Shop Page</h1>
      </div>
    )
  }